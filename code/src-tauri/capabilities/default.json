{
  "identifier": "default",
  "description": "Read-Box 默认权限配置",
  "windows": ["main"],
  "permissions": [
    "core:default",              // Tauri 核心默认权限
    "shell:allow-execute",       // 允许执行外部命令（用于启动 Python 后端）
    "shell:allow-open"           // 允许在文件管理器中打开文件
  ]
}
